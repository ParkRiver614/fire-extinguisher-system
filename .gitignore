# Backend API — 코드는 올리고 비밀값/런타임 산출물만 제외
# (api/.env는 아래 Python 섹션의 `.env` 규칙으로 이미 제외됨)
fire_guard/
api_server_live/api/uploads/

# Raspberry Pi edge — 코드는 올리고 비밀값/런타임 산출물만 제외
FAS-edge/.env
FAS-edge/capture_tmp/
FAS-edge/.pytest_cache/
FAS-edge/*.pt
FAS-edge/reference.jpg

# Python
__pycache__/
*.py[cod]
*.pyo
.env
*.egg-info/

# Node
node_modules/
dist/
.pnpm-store/

# Secrets (contains DB/JWT credentials — see git history purge)
# docker-compose.yml(학과 서버용)은 비번·SECRET_KEY가 평문으로 박혀 있어 계속 제외한다.
# docker-compose.aws.yml은 ${VAR} 치환만 쓰므로 올려도 안전 — 실제 값은 .env.aws에만 둔다.
docker-compose.yml
.env.aws

# IDE
.idea/
.vscode/
*.iml

# Claude
.claude/

# Misc
img/
*.log

# Docs (kept locally only)
docs/
